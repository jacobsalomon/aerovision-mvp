// Global test setup — runs before every test file.
// Adds custom matchers from testing-library so you can write
// things like expect(element).toBeInTheDocument()

import "@testing-library/jest-dom/vitest";
